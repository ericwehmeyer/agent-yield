{"modules": [{"module": "agents.py", "claims": 15, "mismatches": [{"claim": "They are excluded from the brief scoring and from the pass/fail counts, and shown separately", "why": "`render` drops exempt dispatches from `scored` entirely, so they get no row and no call/cost figures anywhere -- only a one-line count of types -- and they are also excluded from the §11 length/cap statistics, not just from brief scoring."}]}, {"module": "boundary.py", "claims": 8, "mismatches": [{"claim": "`--probe`, which records what actually arrives ... and always exits 0", "why": "`main` returns 2 (after printing REFUSAL_PROBE_MESSAGE to stderr) when `--probe` is passed and the armed-refusal sentinel exists, so the probe path can exit 2."}, {"claim": "defaults to ADVISING (exit 0 with the message on stderr and as additionalContext)", "why": "In the advisory path `main` prints the message only as JSON `hookSpecificOutput.additionalContext` on stdout; nothing is written to stderr unless the exit code is 2."}]}, {"module": "cli.py", "claims": 2, "mismatches": [{"claim": "Thin -- every one is a call into a tested module.", "why": "`_list_tags` aggregates per-session `Usage` totals and sorts them, and `_metric_is_empty`/`_empty_metric_note`/`_cmd_report` implement windowing and emptiness logic, all inside cli.py rather than delegating to another module."}]}, {"module": "discovery.py", "claims": 7, "mismatches": [{"claim": "Only `tasks/*.output` under that tree is a transcript.", "why": "`find_transcripts` also collects every `*.jsonl` under any root, including the scratch tree, excluding only paths containing a `scratchpad` component."}]}, {"module": "gate.py", "claims": 8, "mismatches": []}, {"module": "handoff.py", "claims": 4, "mismatches": [{"claim": "The one thing this module writes is the handoff file the caller asks for.", "why": "`consume` also creates a second file by `os.replace`-ing the handoff to `path + ARCHIVE_SUFFIX` (`.loaded`), overwriting any existing archive."}]}, {"module": "ingest.py", "claims": 3, "mismatches": []}, {"module": "interventions.py", "claims": 2, "mismatches": []}, {"module": "modes.py", "claims": 2, "mismatches": []}, {"module": "outcomes.py", "claims": 3, "mismatches": []}, {"module": "predict.py", "claims": 7, "mismatches": []}, {"module": "records.py", "claims": 2, "mismatches": []}, {"module": "report.py", "claims": 3, "mismatches": [{"claim": "The join: spend over outcomes, per mode, with interventions marked.", "why": "Nothing in this module marks interventions on its output -- `render_table` and `render_model_table` emit no intervention column or annotation; `compare_interventions` only returns before/after medians for a caller to render."}]}, {"module": "report_html.py", "claims": 8, "mismatches": []}, {"module": "resume.py", "claims": 12, "mismatches": [{"claim": "It records the payload *keys*, never their values", "why": "`_probe` writes the values of `_VALUE_SAFE_KEYS` -- `payload.get(\"hook_event_name\")` and `payload.get(REASON_KEY)` -- into the probe entry alongside the key list."}, {"claim": "So the silences are named. There are five of them", "why": "Only four of the five names in `DECISIONS` are silences -- the code's own comment says \"The five outcomes. Four of them are silences\" -- since `injected` is the case that prints."}]}, {"module": "session.py", "claims": 5, "mismatches": [{"claim": "Everything unmeasurable is ``None``, never ``0``", "why": "When a transcript yields no main-thread calls, `session_stats` returns `current_context=0` (and `total=Usage.zero()`) rather than `None`, so an unmeasured current context reads as zero."}]}, {"module": "statusline.py", "claims": 12, "mismatches": [{"claim": "the payload wins over the provisional default and the cost bands are computed against the window", "why": "`render` computes the band with `cost_band(current_context)`, which takes absolute tokens and has no window parameter at all; the window is used only for the `current_context / window` percentage."}]}, {"module": "thresholds.py", "claims": 4, "mismatches": [{"claim": "Their call ranges do not overlap at all -- 62-188 versus 4-27", "why": "The module's own recorded corrections below the constants retire that claim, reporting measured un-briefed 3-27 against briefed 3-30 (and 3-27 vs 3-30 after the #32 re-score), i.e. fully overlapping ranges."}]}, {"module": "usage.py", "claims": 2, "mismatches": []}]}
