METHOD. This paragraph is the only thing that differs between the two arms of
this experiment, and it is fixed.

You are the parent, and the parent reads everything. Do the whole audit
yourself: read the modules with your own tools and write the JSON yourself. You
must NOT dispatch any agent -- do not use the `Agent` tool at all, for any part
of this.
